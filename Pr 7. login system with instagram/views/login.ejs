<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f7f7f7;
        }
        .form-container {
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }
        .form-header {
            background-color: #ffffff;
            border-radius: 10px 10px 0 0;
            padding: 20px;
            text-align: center;
        }
        .form-header h2 {
            font-weight: bold;
            color: #262626;
        }
        .form-control {
            border-radius: 5px;
        }
        .btn-login {
            background-color: #0095f6;
            border: none;
            border-radius: 5px;
        }
        .btn-login:hover {
            background-color: #007bbf;
        }
        .footer-link {
            color: #0095f6;
        }
        .footer-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="rounded w-50 mx-auto mt-5 form-container">
        <div class="form-header">
            <h2>Login</h2>
        </div>
        <form action="/loginUser" method="post" class="bg-light p-4">
            <div class="mb-3">
                <label class="form-label">Email</label>
                <input type="email" class="form-control" name="email" required>
            </div>
            <div class="mb-3">
                <label class="form-label">Password</label>
                <input type="password" class="form-control" name="password" required>
            </div>
            <div class="mb-3 d-flex flex-wrap justify-content-between align-items-center">
                <input type="submit" class="form-control w-50 btn-login text-white" value="Login">
                <p class="m-0">Don't have an account? <a class="footer-link" href="/register">Register</a></p>
            </div>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
